apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-portal
  namespace: networking-lab
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hr-portal
  template:
    metadata:
      labels:
        app: hr-portal
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
