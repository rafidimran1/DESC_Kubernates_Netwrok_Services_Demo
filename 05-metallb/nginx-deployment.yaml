apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-lb-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-lb-demo
  template:
    metadata:
      labels:
        app: nginx-lb-demo
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
